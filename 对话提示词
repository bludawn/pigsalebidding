首页页面需要你完成如下功能：
1. 筛选条件中的‘区域’，改为树形选择，层级是 省-市-区、县-镇，对于东莞来说，没有区、县。如果你知道哪里能下载这样的树形数据，告诉我，我去下载。
2. 筛选条件新增‘场点’，‘场点’在‘区域’的左边，场地数据通过接口api获取，打开应用时从后台获取。
3. 去掉'AB票'筛选条件。
4. 筛选条件'产品等级'改成'产品标签'，数据通过接口api获取，打开应用时从后台获取。
  - 这里3、4融合在一起，通过标签来查询和展示。
5. 去掉列表卡片中的 '关注' 按钮
6. 顶部的搜索输入框，支持输入搜索 '场点' 和 '品种'，hint文案为 '搜索场点、品种'
7. 分页拉取数据，每一页20条，滑倒底部上滑拉取下一页数据。
8. 到达底部 则告知已到底部，这个由你来设计，看是文案还是划线等其他方式提示。
9. 上滑到顶部或下滑到底部厚，有回弹的效果。

你先不要动手实现，先告诉我你的接受到的需要，以及打算如何做。


进入到竞价详情页，页面有如下功能：
1. api获取竞价商品详细信息，接口返回如下信息：
  - 图片url、视频url，字符串数组
  - 距离结束时间，倒计时秒数
  - 产品标签，字符串数组
  - 生猪类型名称，字符串
  - 生猪重量区间，字符串数组
  - 场次，字符串
  - 价格，字符串
  - 备注，字符串
  - 起拍数量，正整数
  - 加价幅度，浮点数
  - 加拍价，浮点数
  - 无疫地区，字符串
  - 开票范围，字符串
  - 是否配送，字符串
  - 食料品质，字符串
  - 防疫状态，字符串
  - 竞价须知，字符串
  - 出价明细间隔时间，整数，秒，默认10
2. 进入详情，如果是视频，则默认静音播放
3. 顶部图片和视频介绍，能左右滑动切换
4. 页面字段显示的值，来源对应到api获取的字段
5. 去掉 明细， 设置委托 按钮
6. 在竞价须知上面，新增一个卡片，卡片标题为 出价明细，结构如下：
  -- 从api接口获取 出价明细数据，默认10条，有客户名称、价格、数量、时间 字段，支持获取所有，还是10条
  -- 列表显示出价明细，每一行显示 客户名称、价格、数量、时间
  -- 默认10条，点击底部的展开按钮，获取所有 出价明细 并显示
7. 点击出价后，弹出对话框，显示 生猪类型名称，数量，价格，总价，点击确认后，调用接口api接口完成出价。
8. 完成出价后，刷新出价明细
9. 每隔10秒，刷新一次出价明细，这个时间间隔参数由商品详细信息接口返回。  

以上需求，你先理解下，如果我有没考虑到的，告诉我。

  - 这里有个订单详情页面，订单详情页面，根据入仓不同
  - 这些页面可以通过一个api获取，只是入参不同
  - 待付款，列表显示收款，进入到
  - 代发货
  - 待收货
  - 已完成
  - 全部订单

个人中心页，实现如下功能：
1. 从api获取和显示个人信息：头像、名称、保证金标识、企业名称
2. 从api接口获取 我的订单 数量：待付款、待发货、待收货、已完成、全部订单、我的竞拍
3. 待付款、待发货、待收货、已完成、全部订单，进入到订单商品列表页，通过订单api接口获取列表数据
4. 我的竞拍 进入到我的竞拍页面，有3个页签，竞拍中、竞拍成功、竞拍失败
  - 默认显示竞拍中，通过api获取竞拍中数据，几个页签可以共用一个接口
  - 我的竞拍页面，如何显示你来设计
  - 从我的竞拍进入列表详情页，和从首页竞价列表进入到的竞价详情页一样
5. 地址管理 改名成 收货地址，点击 收货地址 进入到 地址管理页面，默认显示地址列表，通过api接口获取
  - 地址可以通过pcas-code获取，树形选择获取，有联系人、联系电话、省市区、详细地址
  - 可以对地址进行增删改查
  - 地址列表样式、查看样式、编辑样式和常用地址管理页面样式一样，你来设计
  - 以及可以选择默认地址，如果只有一个地址，则为默认地址，默认地址会在竞拍详情页显示
6. 增加一个 总资产 卡片， 通过api接口获取保证金、货款、全部余额，并显示
7. 增加一个 数据统计 卡片， 通过api接口获取 交易总金额、购买总头数，并显示
8. 功能区保留 预付定金、发票管理、地址管理、合同协议，其他的移除
9. 功能区新增 联系我们，点击后 通过api获取 电话、地址、微信二维码，通过弹出对话框提示
10. 保留右上角的设置按钮。

根据以上需求，你理解和整理下，我可能有遗漏的，你可以给出建议，先不要实现。



1. 竞拍详情页，增加2个字段， 总头数 quantity；客户竞拍状态 customerBidStatus类型 MyBidStatus；
2. 我的竞拍 列表不能滑动，定位问题并解决。
3. 个人页面，进入到 地址管理和我的竞拍后，返回到个人页面，页面刷新了，没有保持之前的状态，需要改成保持之前的状态。
4. 从 我的竞拍，进入到 竞拍详情页，返回到我的竞拍列表，有刷新，没有保持之前的状态，需要改成保持之前状态。



现在开始生成 生猪贸易软件系统——企业web管理端 相关功能；
根据erpweb下的modeldesign.md 在管理端生成对应的菜单和和页面功能。
需求如下：
1. 熟读erpweb下的readme.md和CODERULE.md，RuoYi-Vue-v3.9.1目录下的readme.md
2. 在web管理端新家一个大菜单，名字叫“生猪业务”，每个模型都有一个菜单，作为子菜单放在这个下面。
3. 所有业务页面打开的是列表，和ruoyi系统的其他列表一样，有常用的增删改查、排序、导出Excel等操作。
4. 生成的字段注意是f_开头，可能查询时要转换
5. 生成的业务sql放到RuoYi-Vue-v3.9.1/sql/business.sql中，后续我会到数据库执行
你理解以上需求了吗？ 你会怎么做？ 等我确认在动手。




1. 上传下载，本地上传（上传到 html文件夹）
2. 管理端 关联性页面编辑。



基于wepweb/coderule.md规范，web管理端有如下调整：
1. 前端生猪类型，列表能显示 图片、视频。图片、视频点击后能查看。
  - 生猪类型编码固定以'PT-'开头，并且自动按照顺序递增

2. 前端生猪资源，资源编码固定以'RS-'开头，并且自动按照顺序递增

3. 前端竞价商品，商品编码固定以'GD-'开头，并且自动按照顺序递增

4. 前端场点，地址code，通过pcas-code获取，树形选择获取，获取对应code。
  - pcas-code在customer/data目录下，需要我要拷贝到ruoyi-ui目录吗？
  - 场点的经纬度，点击后打开地图选择

5. 前端订单，订单编号固定以'ORDER-'开头，并且自动按照12位长度，从0顺序递增，如000000000001
  - 订单前后端 增加一个 归属企业字段。
  - 送货信息 属于订单的分录，在录入送货信息时，可以直接在订单 新增、修改 页面进行新增和修改送货信息
  - 送货信息，通过卡片显示，并在卡片中显示位置地图

6. 前端送货信息，前后端 新增 运输编码 字段，输入固定以'TS-'开头，并且自动按照顺序递增
  - 前后端 新增 当前位置经纬度 字段，点击后打开地图选择

7. 前端企业信息，列表能显示营业执照图片，
